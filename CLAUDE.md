# PeakTrail — Guía para Claude Code

App de montañismo del Pirineo (ibones, picos, collados, refugios, rutas GR/PR/SL) sobre mapa 2.5D, para un grupo cerrado de ~10 amigos. El plan de desarrollo completo está en `planning.md`: fase 1 (hitos 0–10, completada salvo el 9) y fase 2 (hitos 11–21, del re-planning de julio de 2026) — consúltalo antes de empezar cualquier hito. Las mejoras diferidas que surjan al implementar se apuntan en `notas-mejoras.md`.

## Estado

- **Hito 0 completado**: Next.js 15 (App Router) + TypeScript + TailwindCSS 4, SDK de Firebase integrado (`src/lib/firebase.ts`, inicialización condicionada a env vars), devcontainer para Codespaces.
- **Hito 1 completado**: mapa 2.5D con MapLibre GL JS (`src/components/map/`): ortofoto PNOA y topónimos IGNBaseOrto vía WMTS del IGN, terreno 3D con teselas terrarium de Mapzen/AWS (EU-DEM/SRTM; cambiar por tubería propia MDT05 → terrain-RGB si se quiere más resolución), hillshade, cielo/niebla, controles propios (zoom, brújula, 2D/3D, topónimos) y atribución visible. Design tokens de la paleta Pirineo en `globals.css` (@theme de Tailwind 4), tipografía Fraunces + Archivo, set de iconos propio en `src/components/icons.tsx`.
- **Hito 2 completado**: modelo de datos en `src/types/catalogo.ts`; catálogo generado en `data/catalogo.json` (350 elementos: 153 tresmiles, 166 ibones, 31 refugios) vía `npm run catalogo:generar` (Overpass/OSM, provincia de Huesca, lat ≥ 42.3, con ajustes manuales en `data/curados.json`); importado a Firestore (colección `elementos`, doc id = slug, `npm run catalogo:importar`, reimportable sin duplicar). Firebase y Vercel ya configurados por el usuario.
- **Hito 3 completado**: marcadores por tipo sobre el mapa (capa de símbolos con insignias SVG rasterizadas desde los trazos compartidos de `src/components/iconos-trazos.ts`, color por tipo, prioridad de colisión por altitud), ficha lateral al hacer clic (`FichaElemento.tsx`) y filtros-leyenda por tipo con recuentos. La app lee `data/catalogo.json` estático; Firestore queda para datos de usuario (hitos 6-8).
- **Hito 4 completado**: rutas de senderismo desde OSM (`npm run rutas:generar` → `public/rutas.json`, 248 rutas: 85 GR, 130 PR, 33 SL; cose tramos, muestrea elevación del DEM terrarium cada 50 m con pngjs, calcula distancia/desniveles con histéresis de 5 m, simplifica con Douglas-Peucker y descarta rutas con <60 % del trazado en lat ≥ 42.3). Capas de línea por red con colores validados (GR #c26a3f, PR #c98500, SL #55a05e), capa invisible ancha para el clic, resaltado de la seleccionada, ficha con métricas y perfil de elevación SVG interactivo (`PerfilElevacion.tsx`, serie #3f92c9), filtros-leyenda por red.
- **Hito 5 completado**: tiempo de marcha estimado con la función de Tobler por tramos del perfil y valoración MIDE estimada (esfuerzo por la escala oficial de horas; medio/itinerario/desplazamiento heurísticos por altitud), en `src/lib/metricas-ruta.ts` y visibles en la ficha de ruta. Botón «Invertir sentido» en la ficha: voltea perfil, desniveles, extremos y recalcula tiempo/MIDE (`invertirRuta` en `src/components/map/rutas.ts`). Señales de salida (punto-anilla) y llegada (banderín) en claro sobre el trazado seleccionado, y punto azul en la ruta sincronizado con el cursor del perfil.
- **Hito 6 completado**: planificador de rutas propio (botón de trazado en los controles): clics sobre el mapa que se enganchan a senderos con el OSRM a pie de FOSSGIS (`src/lib/enrutador.ts`, con respaldo a línea recta y modo «línea recta» manual), métricas y perfil en vivo muestreando elevación en el navegador (`src/lib/elevacion.ts`, mismas teselas terrarium), deshacer/limpiar, y guardado en Firestore (colección `planes`, `src/lib/planes.ts` — sin arrays anidados: coordenadas como arrays de mapas) con listado, vista y borrado en el panel (`Planificador.tsx`). Ojo: `line-dasharray` + terreno 3D congela MapLibre (documentado en `MapView.tsx`); en dev el mapa se expone como `window.__mapa` para depurar.
- **Hito 7 completado**: cuentas con Google (Firebase Auth, `src/lib/auth.ts`) y lista cerrada en la colección `invitados` (`npm run invitados:anadir -- email`; josemiangos@gmail.com ya sembrado); marcar elementos/rutas como realizados con fecha y notas desde las fichas (`MarcarRealizado.tsx`, colección `realizados` con id determinista usuario__tipo__ref, escucha en vivo); panel de progreso personal (barras por tipo) y ranking del grupo (`Progreso.tsx`); planes etiquetados con su autor. Reglas de seguridad listas en `firestore.rules` (pendiente de pegarlas en la consola).
- **Gestión de invitados desde la app**: rol admin (campo `admin: true` en su doc de `invitados`; josemiangos@gmail.com ya lo tiene) con sección «Invitaciones» en el panel de progreso para invitar/expulsar por correo (`src/lib/invitados.ts`), permitido por `firestore.rules` incluso con el modo de prueba cerrado. `npm run invitados:anadir -- --admin email` para nombrar más admins.
- **Pendiente del usuario (Hito 7)**: proveedor Google y dominios autorizados hechos; falta aplicar `firestore.rules` en la consola para cerrar el modo de prueba (los invitados ya se gestionan desde la app).
- **Hito 8 completado**: integración con Strava. OAuth2 con route handlers (`src/app/api/strava/`: callback que intercambia el código con el secreto en servidor, refresh de tokens y proxy de actividades porque el API de Strava no permite CORS); tokens por dispositivo en localStorage (`src/lib/strava.ts`); emparejado geográfico en cliente (`src/lib/emparejar.ts`: polilinea decodificada + rejilla; elemento si la traza pasa a <150 m, ruta si ≥70 % de sus puntos quedan a <250 m) que marca realizados idempotentes con fecha de la actividad y nota «Strava: …». Sección en el panel de progreso (conectar/importar/desconectar, solo deportes de montaña). Requiere `NEXT_PUBLIC_STRAVA_CLIENT_ID` y `STRAVA_CLIENT_SECRET` (app creada en strava.com/settings/api con el dominio de callback correcto).
- **Pendiente del usuario (Hito 8)**: crear la app en Strava, rellenar las dos variables en `.env.local` y en Vercel, y probar el ciclo conectar → importar.
- **Hito 9 APLAZADO** a petición del usuario (offline no prioritario en fase de desarrollo).
- **Hito 10 completado**: ambientes del mapa día/niebla/atardecer (presets de cielo, sombreado y ortofoto en `mapStyle.ts`, botón en controles); búsqueda global por nombre de elementos y rutas (`Buscador.tsx`, en la fila de filtros, vuela y abre la ficha); favicon SVG propio (`src/app/icon.svg`); fotos del grupo por elemento/ruta (comprimidas en cliente a JPEG <900 KB y guardadas como data-URL en Firestore —Storage exige Blaze—, galería con visor y borrado propio) y comentarios en las fichas (`src/lib/social.ts`, `SeccionSocial.tsx`); `firestore.rules` ampliadas con `fotos` y `comentarios` (re-aplicar en la consola si ya se pegaron).
- **Re-planning hecho (julio de 2026)**: fase 2 definida en `planning.md` con el usuario. Decisiones: todos los picos con nombre y cota (no solo tresmiles) con filtro de altitud en la UI, collados como cuarto tipo, ámbito ampliado a todo el Pirineo peninsular (Navarra + Aragón + Cataluña) en catálogo y rutas, fidelidad con MDT05, UI con panel desplegable de capas, estadísticas de realización rediseñadas, y extras (meteo, compartir, GPX, explorador, Strava II, Wikidata, social II) en hitos pequeños 15–21. Sitios de interés (miradores, cascadas, cuevas) apuntados en `notas-mejoras.md` para después.
- **Hito 11 completado**: panel «Capas y filtros» desplegable (`PanelCapas.tsx`): agrupa catálogo, senderos y ajustes de mapa (topónimos y selector de ambiente con etiquetas, que salen de los controles laterales), con punto ocre cuando hay capas ocultas y cierre al pulsar fuera; la fila superior queda en buscador + botón. Preferencias de vista (filtros, ambiente, topónimos) persistidas en localStorage (`src/lib/preferencias.ts`), restauradas tras montar (no en el render, por la hidratación) y aplicadas al mapa vía efectos (ambiente y topónimos se convirtieron en efectos sobre `[valor, cargado]`). Ojo z-index: la fila del buscador lleva `z-10` (el desplegable debe pasar sobre las fichas) y el velo de carga `z-30` para seguir tapando la UI mientras el mapa carga.
- **Hito 12 completado**: catálogo total del Pirineo peninsular. `npm run catalogo:generar` consulta ahora tres zonas (Aragón/Navarra/Cataluña por código ISO3166-2, admin_level 4, con cortes de latitud 42.3/42.6/42.1) y trae 9.730 elementos: picos con nombre y cota (6.926), collados/puertos con nombre (2.159, `natural=saddle` + `mountain_pass=yes`, cota opcional), ibones y estanys (549, regex ibón/estany/estanh; los embalses se excluyen por nombre, no por `water=reservoir`, que también llevan Estany Gento) y refugios (96). El catálogo vive en `public/catalogo.json` (minificado, ~1.9 MB) y se descarga en el `load` del mapa (`cargarCatalogo()` en `elementos.ts`, que rellena `elementosPorId`/`TOTALES` — seguro porque el velo z-30 bloquea la UI hasta entonces); ya no viaja en el bundle JS. Collado como cuarto tipo visible: color malva #b48ead (validado en CVD contra los otros tres con el skill de dataviz; los marcadores llevan glifo propio como codificación secundaria), marcador+destello autogenerados, fila en el panel, ficha y progreso. Deslizador de cota mínima de picos en el panel (0–3.000 m, paso 100, solo recorta picos; muestra el recuento visible) persistido en preferencias (clave v2 `peaktrail:vista:2`, porque las v1 no conocían el collado). Etiquetas «Tresmiles»→«Picos», cabecera «Pirineo». Ojo entorno: `lsof` no funciona en el sandbox (matar `next start` por PID con `ps`), y dos `npm run build` seguidos con el servidor vivo dejan HTML viejo en memoria (EADDRINUSE silencioso en server.log).
- **Hito 13 completado**: rutas ampliadas al Pirineo peninsular y fidelidad con el MDT05. `npm run rutas:generar` consulta ahora las tres zonas (Aragón/Navarra/Cataluña, una petición Overpass por zona y por red GR/PR/SL para no saturar el servidor) y deduplica por id de relación las que cruzan de comunidad (GR-11, HRP); trae 1.008 rutas (310 GR, 343 PR, 355 SL, antes 248 solo de Huesca). El perfil, desniveles y cotas del catálogo (`scripts/mdt05.mjs`) se calculan ahora con el **MDT05 del IGN** (malla de 5 m, LiDAR PNOA-LiDAR) vía su servicio WCS 2.0 (`servicios.idee.es/wcs-inspire/mdt`, coverage `Elevacion4258_5` en ETRS89 geográficas, sin reproyectar) en vez de terrarium (~25-30 m); se consulta por celdas de 0,02° cacheadas en disco (`.cache/mdt05`, fuera del repo) y en memoria, igual que las teselas terrarium. El terreno 3D visual del mapa sigue con terrarium (Hito 1), sin tocar. Los picos toman el máximo en una ventana de ±2 px porque el nodo de OSM rara vez cae en el píxel exacto de la cumbre (`altitudPicoMDT05`); las rutas que se asoman a Francia (HRP, GR-10, GRT...) usan terrarium como respaldo (`scripts/terrarium.mjs`) porque el MDT05 solo cubre España. Overpass compartido en `scripts/overpass.mjs` (mismo cliente con reintentos para catálogo y rutas). Generación resiliente a cortes: `generar-rutas.mjs` guarda un punto de control en disco (relaciones de Overpass y rutas ya procesadas) cada 20 relaciones y al recibir SIGTERM/SIGINT, y lo retoma solo si lo encuentra; se borra al terminar con éxito.
  - **Bug de nodata encontrado y corregido**: el WCS del MDT05 rellena con **0** (no un centinela negativo) los píxeles fuera de España; `0 ?? respaldo` en JS no dispara el operador porque `??` solo sustituye `null`/`undefined`, así que el 0 se colaba como cota válida (HRP con tramos a 0 m y desniveles disparatados, 3 ibones costeros de Girona con altitud 0). Corregido tratando `valor <= 0` como nodata en `mdt05.mjs` (ningún punto real del catálogo/rutas está al nivel del mar).
  - **Entorno muy inestable durante este hito**: el Codespace se reinició o se quedó suspendido varias veces en plena generación (una vez con salto de reloj de más de 8 h), y Overpass tuvo rachas de 429/504. El checkpointing fue lo que permitió terminar sin perder el trabajo ya hecho; si un hito futuro implica un script largo (>5 min), añadir punto de control desde el principio en vez de esperar a que falle.
- **Hito 14 completado**: estadísticas de realización rediseñadas en el panel de progreso (`Progreso.tsx`), con apartados dedicados. «Lo mío» se mantiene (barras por tipo + rutas, ahora con el componente `BarraProgreso` compartido) y se añaden: «Picos por altitud» (mismas 4 bandas que el deslizador del Hito 12: <2.000, 2.000-2.499, 2.500-2.999, ≥3.000 m, hechos/total por banda calculado cruzando `realizados` con `elementosPorId`); «Pendientes» (tiles con icono: tresmiles, collados, ibones, refugios y rutas que faltan, `TOTALES - propios`); «Evolución del grupo» (histograma SVG propio de actividades marcadas por mes en el último año, `GraficoActividad.tsx`, en el color de marca ocre, coherente con `PerfilElevacion.tsx`); «El grupo» se mantiene igual (ranking). Verificado con build/lint/tipos y una comprobación visual del estado vacío/sin sesión (no se ha escrito en la Firestore real del grupo para no alterar datos compartidos, así que las secciones personales con datos reales no se vieron en pantalla, solo se revisó su lógica en el código).
- **Ampliación del Hito 14 — reparto por comunidad**: a petición del usuario, catálogo y rutas llevan ahora un campo `comunidad` (`aragon` | `navarra` | `cataluna`, tipo `Comunidad` en `src/types/catalogo.ts`) asignado en la generación (`zona.clave` en `generar-catalogo.mjs`; en rutas, la primera zona en la que Overpass devolvió la relación, acumulando todas las que toca en `rel._comunidades` para las transfronterizas como el GR-11 — `generar-rutas.mjs`). Nueva sección «Por comunidad» en `Progreso.tsx` con una barra por comunidad (elementos+rutas combinados, color hielo-500 neutro). El reparto más fino por comarca (Sobrarbe, Ribagorza...) queda apuntado como Hito 22 futuro en `planning.md` y en `notas-mejoras.md` — la comunidad es una aproximación deliberadamente simple mientras tanto.
- **Hito 15 completado**: previsión meteo de 7 días en las fichas de elemento y de ruta. `src/lib/meteo.ts` consulta Open-Meteo (gratuito, sin clave) con la **cota del elemento** en el parámetro `elevation` (temperatura ajustada a altura); trae por día código WMO, temp máx/mín, precipitación y su probabilidad, nevada y viento máximo, más el **nivel de congelación** horario del que se deriva la cota de nieve (mínimo del día, redondeada a 50 m). Respuesta cacheada en memoria por punto redondeado (~1 km) y cota. Componente `Meteo.tsx`: tira de 7 días seleccionable + detalle del día elegido (precipitación, viento, cota de nieve, nevada); en la ficha de ruta usa el punto medio del trazado. Iconos meteo propios (`IconoMeteo` por estado del cielo — sol, nubes, niebla, lluvia, chubascos, nieve, tormenta — e `IconoViento`) en el set SVG, sin librerías ni emojis.
- **Hito 16 completado**: compartir por URL y buscador con ranking. La URL refleja la ficha abierta (`?el=<id>` / `?ruta=<id>`) vía `history.replaceState`, y al cargar se abre la que venga en la URL (un efecto en `MapView.tsx` con guarda `deepLinkAplicado`, tras esperar a que catálogo y rutas estén cargados). Botón «Compartir» en la cabecera de ambas fichas que copia el enlace al portapapeles (`BotonCompartir.tsx`, `IconoCompartir` propio, respaldo con `prompt`). Buscador (`Buscador.tsx`) con puntuación en vez de solo «contiene»: prefijo del nombre (100) > prefijo de palabra (80) > contiene (60, penalizando la posición) > **tolerancia a erratas** por distancia de edición acotada (Levenshtein, `q≥5`→2, `q≥3`→1) contra el arranque de cada palabra (40); orden por puntuación y, a igualdad, por nombre más corto.
- **Hito 17 completado**: (a) **revisión a fondo del planificador** de rutas propias, que era tosco y fallaba, y (b) GPX.
  - **Enganche a senderos reales sin conectores rectos**: `enrutarSegmento` (`src/lib/enrutador.ts`) devuelve la geometría del OSRM a pie tal cual **y los extremos ya enganchados** al sendero (`waypoints[].location`); si el enganche queda a >400 m del clic (zona sin sendas) o el servicio falla, degrada a línea recta conservando el punto. Se acabaron las líneas rectas del clic al sendero que ensuciaban el trazado.
  - **Recálculo robusto** (adiós al desfase índice-a-índice anterior): modelo de `Waypoint` con id (`src/types/plan.ts`); un efecto recalcula todos los segmentos pero **cachea por (modo, par de puntos)** en `cacheSegmentos` y cancela con token, así que al añadir/mover/reordenar solo se rehacen los afectados. El trazado se cose pasando por «nodos» comunes para quedar continuo aunque se mezclen tramos por sendero y rectos.
  - **Puntos ordenados, arrastrables, con salida/llegada**: marcadores **DOM** de MapLibre numerados (`elementoWaypoint`), arrastrables (`dragend` → reengancha), con salida (1) en pino y llegada (N) en ocre; no se usó `text-field` porque el estilo IGN no trae `glyphs`. Panel (`Planificador.tsx`) con lista de puntos: reordenar con flechas y borrar individual, además de deshacer/limpiar. Los planes guardados muestran salida (anilla) y llegada (banderín) por icono-por-dato en la capa de puntos.
  - **GPX** (`src/lib/gpx.ts`): exporta GPX 1.1 con la traza + salida/llegada como waypoints y **elevación interpolada del perfil** por distancia acumulada; disponible en la ficha de ruta (`rutaCatalogoAGpx`), en los planes guardados (`planAGpx`) y en el borrador (`construirGpx` sobre el trazado en vivo). Importa (`leerGpx`, `DOMParser`) la primera traza/ruta/waypoints; en el planificador la traza importada **se conserva íntegra** sembrando la caché de segmentos en modo recto (dos puntos editables, trazado idéntico al archivo) y encuadra el mapa. Iconos `IconoDescargar`/`IconoImportar` propios. Verificado el ida-y-vuelta (export bien formado, cota coherente con el perfil, reimportación con las mismas coordenadas). La interacción del mapa no se probó en headless por el congelado conocido de SwiftShader tras el `load`.
- **Mejora post-Hito 17 — enrutado profesional del planificador (BRouter + Turf)**: a raíz de puntos mal colocados alrededor del refugio de Góriz, se encontró la **causa raíz**: el perfil `foot` del OSRM público **excluye del grafo los caminos con `sac_scale` > hiking**, que es casi todo en alta montaña — enganchaba los clics a caminos «fáciles» a >1 km (medido: enganche a 1.492 m del refugio y «ruta» absurda de 125 m; BRouter engancha a 8 m y da los 2,5 km reales por senda).
  - **Motor nuevo**: BRouter (`brouter.de`, perfil `hiking-mountain`, sin clave, CORS abierto, ~90 ms por segmento) como principal; OSRM a pie queda de **respaldo** y, si ambos fallan, línea recta — cascada con timeout por `AbortController` en `src/lib/enrutador.ts`. BRouter devuelve elevación por vértice (hoy se descarta; apuntado en notas).
  - **Extremos predecibles (estilo Komoot)**: clic a ≤45 m del camino (`UMBRAL_SNAP_M`) → el punto se **adhiere** al camino; más lejos → el punto se queda **exactamente donde se clicó** y se une con un conector recto explícito (`componerSegmento`). La geometría siempre empieza/termina en el extremo visible: la línea llega al marcador **por construcción** (se acabaron los marcadores sueltos y los puntos «recolocados en otro lado»).
  - **Salvaguarda de rodeo** (`FACTOR_RODEO_MAX`): si la ruta por senderos supera 3× la distancia directa **y** se aleja del tramo más que la propia directa (mín. 150 m), el segmento degrada a recta. Mata el patrón «peine» de puntos campo a través (bajar a la senda del valle y volver a subir por cada par de puntos; medido en la ladera de Góriz: 4,6 km por red para 367 m directos porque dos sendas próximas no conectan en el grafo OSM), pero respeta rodeos legítimos largos y *pegados* al tramo (zigzags de ladera, bordear un circo).
  - **Lógica pura extraída a `src/lib/plan.ts`** (sin mapa ni React, testeable): `claveSegmento`, `nodosDelPlan`, `coserTrazado` e `indiceDeInsercion` (Turf `nearestPointOnLine`, paquetes modulares `@turf/nearest-point-on-line` + `@turf/helpers`, no el metapaquete).
  - **Insertar punto en mitad de la ruta**: pulsar sobre la línea del borrador (franja invisible `plan-pulsable`, mismo patrón que `rutas-pulsable`) inserta el waypoint entre los dos puntos de ese tramo en vez de añadirlo al final.
  - **Batería de pruebas con vitest** (nueva infra: `vitest.config.ts`, carpeta `pruebas/`): `npm test` corre 24 unitarias sin red (cosido, nodos, inserción, snap/conector, cascada con fetch mockeado, degradación sin conexión); `npm run test:red` añade 6 de **integración real** contra BRouter con casos del Pirineo (Góriz→Paso de Góriz por senda real, GR-11 con enganches finos, clic fuera de senda que se queda exacto, continuidad sin saltos >600 m). 30/30 en verde.
- **Hito 18 completado**: explorador del catálogo (`Explorador.tsx`, botón de lista en los controles): pestañas por tipo más rutas, columnas ordenables (nombre, altitud/km, hecho), filtro por nombre, selector de comunidad y filtro hechos/pendientes con sesión; la fila vuela al elemento y abre su ficha. Paginado con «Mostrar más» (150+300) para los ~6.900 picos. Cierre mutuo con progreso/planificador/fichas.
- **Hito 19 completado**: Strava, segunda ronda, con la **capa «Mis actividades»** pedida por el usuario como pieza principal:
  - **Capa en el mapa**: las salidas de Strava del usuario como una «red» más de líneas (azul `#3f92c9` —el de la serie de elevación—, **validado en CVD** con el validador del skill de dataviz contra GR/PR/SL: ΔE mínimo del conjunto 24,6, muy sobre el umbral 12; frente al marcador de ibones la codificación secundaria es el tipo de marca). Fuente + casco + franja pulsable (`actividades-capa.ts`), entre las rutas y los marcadores. Activable en el panel «Capas y filtros» (sección «Mis actividades», solo si hay caché) y **persistida** en preferencias (`mostrarActividades`).
  - **Ficha de actividad** (`FichaActividad.tsx`) al pulsar una traza: datos del API (distancia, tiempo en movimiento, desnivel +, ritmo, fecha, deporte etiquetado en ES), **perfil de elevación muestreado de nuestro DEM** sobre la traza, descarga GPX y enlace a strava.com. Sin deep-link en la URL (las actividades son por dispositivo, el enlace no valdría para otros).
  - **Importación incremental** (`src/lib/actividades.ts`): caché localStorage de las actividades de montaña con traza + `ultimaEpoca`; al importar se pide a Strava solo lo posterior (`after=`, el proxy ya lo pasaba) y **solo las nuevas se emparejan**. La caché alimenta la capa; desconectar la borra.
  - **Emparejar planes propios**: `emparejarTraza` acepta planes (cobertura 70 %/250 m como rutas); tipo `"plan"` añadido a `Realizado` (las reglas de Firestore no restringen el campo, no hay que tocarlas) y marca «hecha» (check pino) en la lista de planes del planificador (`planesHechos` derivado de realizados).
  - **Calibrar umbrales** (150/250 m, 70 %) queda pendiente de salidas reales del grupo, como estaba anotado.
  - **Remates pedidos por el usuario**: pestaña **«Salidas»** en el explorador (solo con caché de actividades; columnas nombre/km/fecha ordenables, sin filtros de comunidad/estado que no aplican; la fila enciende la capa, selecciona la actividad y encuadra su traza — `verActividad` activa `mostrarActividades` para que el efecto de limpieza no anule la selección). **Salida/llegada** (anilla/banderín, iconos existentes) sobre la traza de la actividad seleccionada más resaltado blanco (`actividad-destacada`, filtro por id). **Cursor de pendiente en el perfil** (punto azul sincronizado al pasar el ratón, como en las rutas del catálogo) también para el borrador/planes del planificador y las actividades de Strava: helpers genéricos `puntoEnLinea`/`extremosLinea`/`limitesLinea` en `rutas.ts` (probados en `pruebas/lineas.test.ts`). Filtros de capa por año/deporte y trazas compartidas del grupo quedan en `notas-mejoras.md` como mejora futura.
- **Hito 20 completado**: descripción y foto libre desde Wikidata/Wikipedia en la ficha de elemento, para lo que ya trae la etiqueta `wikidata` en OSM (2.679 de 9.756 elementos, ~27 %; picos 2.155, ibones 248, collados 223, refugios 53 — se captura sin más coste en `generar-catalogo.mjs` porque `out tags` de Overpass ya la traía, solo había que leerla; nuevo campo opcional `wikidata` en `ElementoGeografico`, no se resuelve el caso wikipedia-sin-wikidata por ser solo 1 de ~3000 picos en Aragón). Todo en cliente y en vivo (nada se guarda en el catálogo salvo el QID): `src/lib/wikipedia.ts` resuelve el QID a sitelinks con la API de Wikidata (`origin=*` habilita CORS) y prueba idiomas en orden de preferencia (`es → ca → oc → eu → fr → en`, las cooficiales del Pirineo peninsular antes que inglés) hasta encontrar un artículo con extracto; la API REST `page/summary` de esa Wikipedia da el extracto y la imagen de portada (también CORS abierto). Cacheado en memoria por QID. `SeccionWikipedia.tsx` no renderiza nada si el elemento no tiene `wikidata` o no hay resumen aprovechable (páginas de desambiguación descartadas), para no dejar un hueco vacío en la ficha. **Bug encontrado en revisión**: `sitelinksDe` lanzaba si Wikidata respondía con error HTTP y nada lo capturaba (promesa rechazada en vez de contenido opcional degradando a nada); corregido envolviendo `obtenerResumen` en try/catch.
- **Ampliación del Hito 20 — traducción automática**: a petición del usuario (muchos picos solo tienen artículo en catalán/francés/etc., y quedaban así en la ficha), el extracto se traduce siempre al castellano cuando el artículo encontrado no está ya en `es`. Motor: **MyMemory** (`api.mymemory.translated.net`, gratuita, sin clave, CORS abierto). Trocea el texto por frases bajo 480 caracteres (`trocearParaTraducir`) porque MyMemory rechaza consultas de más de 500 con 403; si cualquier trozo falla, se descarta la traducción entera y se muestra el extracto original (mejor un texto en catalán que uno a medio traducir mezclando idiomas). Nota de transparencia junto al enlace («Traducción automática del catalán · Leer en Wikipedia», o «En occitano (no se pudo traducir) · Leer en Wikipedia» si falló). **Calidad por idioma, comprobada con textos reales**: catalán, francés y euskera traducen bien (verificado con Cotiella, Mont Perdu, Anboto); **occitano da resultados incoherentes** (par de idiomas poco entrenado en el motor) — se traduce igualmente pero la nota de transparencia avisa y enlaza al original para que el usuario pueda comprobarlo. 10 pruebas unitarias (`pruebas/wikipedia.test.ts`, antes 7): no llama al traductor si ya está en castellano (ahorra cuota), fallback al original si la traducción falla, troceo y unión de extractos largos. Verificado también en real (Agulla de Saboredo, catalán → castellano, topónimos como «Pallars Sobirà» conservados sin traducir).
- **Hito 21 completado**: social, segunda ronda. `src/lib/social.ts` reescrito con `escucharSocial` (`onSnapshot`, dos queries por refTipo+refId —dos igualdades, sin índice compuesto—, una por colección) en vez del `getDocs` puntual de antes: comentarios y fotos se ven llegar en vivo, propios y del resto del grupo, sin reabrir la ficha ni llamar a un `recargar()` manual tras cada escritura (`SeccionSocial.tsx` simplificado en consecuencia). Expone `listo` (ambas colecciones han entregado ya su primer snapshot) en vez de marcar «cargado» en cuanto responde la primera de las dos, que dejaba parpadear un «sin fotos todavía» falso mientras la otra aún resolvía. **Miniaturas**: `subirFoto` genera ahora también una versión pequeña (320 px, <30 KB, `comprimirMejorEsfuerzo` sin lanzar si no llega al objetivo) además de la grande (1280 px, <900 KB, `comprimirEstricto`, sigue lanzando si no cabe); la rejilla de la galería pinta `foto.miniatura ?? foto.datos` (las fotos de antes de este hito no tienen miniatura y siguen funcionando con el original), y el visor ampliado sigue usando `datos`. Sin cambios en `firestore.rules` (el campo extra no está restringido). Verificado con tipos/lint/build/tests; no se probó el flujo real (login/subida/vivo) porque exige una cuenta de invitado real y no se debe escribir en la Firestore compartida del grupo solo para probar — mismo criterio que en el Hito 14.
- **Hito 9 completado** (retomado tras la fase 2, a petición del usuario: es la mejora con más impacto real en el monte y sienta la base técnica para la futura app móvil): PWA instalable + caché offline, en dos checkpoints.
  - **Cimientos**: Firestore con persistencia offline real —`src/lib/firebase.ts` pasa de `getFirestore()` fresco en cada `getDb()` a una única instancia memoizada de `initializeFirestore` con `persistentLocalCache`/`persistentMultipleTabManager` (API modular de Firebase JS SDK v11); cambio de ~10 líneas que arregla `realizados.ts`/`planes.ts`/`social.ts` sin tocarlos: sus `onSnapshot` sirven el último snapshot cacheado sin red y las escrituras quedan en cola local hasta reconectar. `src/app/manifest.ts` (convención tipada de Next 15) + `src/app/apple-icon.png` + `export const viewport` en `layout.tsx`; iconos rasterizados desde el `icon.svg` propio del Hito 10 con un `scripts/generar-iconos.mjs` nuevo (`sharp`, ya presente transitivamente — `npm run iconos:generar`), con variantes "maskable" (glifo al 72% sobre fondo sólido, para el recorte circular de Android). Service Worker escrito a mano (`public/sw.js`, sin Workbox/next-pwa/@serwist: la pieza específica de este hito —descarga explícita de teselas con progreso y borrado selectivo— es lógica de aplicación que hay que escribir igual exista o no una librería debajo, mismo criterio que el cliente Overpass o el emparejado de rutas propios): cachés versionadas para el shell (`/`) y `catalogo.json`/`rutas.json` (stale-while-revalidate, se purgan al activar una versión nueva del SW) + una caché sin versión para teselas vistas de pasada (`peaktrail-teselas`, cache-first) que nunca se toca en el barrido de `activate`. `next.config.ts` fuerza `Cache-Control: no-cache` en `/sw.js` para que las actualizaciones se detecten pronto. Registro vía `src/components/RegistrarSW.tsx` (client component montado en `layout.tsx`).
  - **Descarga explícita "para sin cobertura"**: `src/lib/geo.ts` nuevo (extraído de `elevacion.ts`: `haversine`, `remuestrear`, `teselaFraccional`, ahora compartidos) + `src/lib/teselas.ts` (calcula las teselas de pnoa/topónimos/terreno que cubren el corredor de una línea remuestreando cada 150 m y añadiendo el vecindario 3×3 de cada punto, sin añadir `@turf/buffer` — a z16 una tesela mide ~450 m a latitud pirenaica, de sobra para un corredor de 300-400 m; probado con una línea recta y una «herradura» que confirma que no se descarga el bbox completo). `src/lib/descargas.ts` gestiona los "packs" descargados en `localStorage` (mismo patrón que `actividades.ts`/`preferencias.ts`, no IndexedDB): descarga con concurrencia acotada a la caché `peaktrail-teselas-packs` (sin versión, solo se borra por acción del usuario) y borra un pack sin tocar teselas que otro pack superviviente todavía use. Icono propio `IconoDescargaOffline` (rejilla de teselas + flecha, distinto de `IconoDescargar` que es específicamente GPX); botón nuevo en la fila de control del mapa junto a Planificador/Progreso/Explorador que abre `PanelDescargas.tsx` (lista de packs con tamaño aproximado, fecha y borrado, más `navigator.storage.estimate()` para la cuota real del dispositivo); botón «Descargar para sin cobertura» (`BotonDescargaOffline.tsx`, compartido) en `FichaRuta.tsx`, `FichaActividad.tsx` y `Planificador.tsx` (plan guardado). `src/lib/conexion.ts` (`useConexion()` sobre `navigator.onLine`) para que Meteo, Wikipedia, sincronizar Strava y el planificador expliquen con un aviso por qué no funcionan sin red, en vez de fallar en silencio o feo (el enrutador BRouter/OSRM ya degradaba a línea recta desde la mejora post-Hito 17; esto es solo la comunicación al usuario de *por qué*); pastilla «Sin conexión» en la cabecera.
  - Verificado con tipos/lint/build/tests (5 pruebas nuevas de `teselas.ts`) y arrancando `next start` en local (`curl` a `/manifest.webmanifest` y `/sw.js` con sus cabeceras, iconos servidos). **No verificado aquí** (a probar el usuario en dispositivo real, mismo criterio que con Firestore real en hitos anteriores): instalación «Añadir a pantalla de inicio» en iOS/Android, modo avión tras recargar con un pack ya descargado, `navigator.storage.estimate()` con cuota real, y que Firestore sirva de verdad en caché sin red.
- **Panel de progreso reorganizado en desplegables** (a petición del usuario, antes de plantear la fase 3 a fondo): `Progreso.tsx` ya no apila todas las secciones abiertas a la vez. Nuevo componente `Desplegable` (cabecera con título + resumen de una cifra siempre visible, incluso cerrada, y chevron `IconoDespliegue` que rota) envuelve «Lo mío» (abierta por defecto), «Por comunidad», «Picos por altitud», «Pendientes», «Evolución del grupo» y «El grupo». «Por comunidad» pasa de listar las 3 barras a la vez a un selector por pestañas (`PorComunidad`, mismo patrón `role="tablist"` que las pestañas del Explorador): se elige una comunidad y solo se ve su barra — pensado para que el Hito 22 anide comarcas dentro de la comunidad elegida con el mismo patrón en vez de añadir más filas sueltas.
- **Sistema de grupos de amigos, checkpoint 1 de 2 (aditivo, nada roto)**: diseño completo en dos checkpoints (plan revisado con el usuario tras explorar a fondo el modelo de datos actual). Decisiones: datos (realizados/planes/social) separados por grupo, no solo organizativos; roster global de «amigos» (colección `amigos`, sustituye a `invitados`) que se asigna a uno o varios grupos; rol **propietario** fijo (email constante en `src/lib/propietario.ts` y en `firestore.rules`, sin ruta en la app para crearlo) que es el único que puede ascender/descender **admins**; los admins crean grupos y gestionan miembros pero nunca pueden nombrar a otro admin (ni siquiera el botón existe en su UI); sin migración de los datos actuales (aceptado por el usuario, arranque limpio). Este checkpoint 1 añade, sin tocar nada que ya funcione: `src/types/grupo.ts`, `src/lib/propietario.ts`, `src/lib/amigos.ts`, `src/lib/grupos.ts` (miembros como array en el propio documento del grupo, no subcolección — de sobra a esta escala; `arrayUnion`/`arrayRemove` para altas/bajas atómicas); `firestore.rules` gana `esPropietario()`, `esAdminGrupos()`, `miembroDeGrupo()` y los bloques `amigos`/`grupos`, dejando intactos `invitado()`/`esAdmin()`/`invitados` de siempre; `EstadoUsuario` (`auth.ts`) gana `amigo`/`adminGrupos`/`propietario`/`grupos` junto a los campos viejos; nuevos `PanelAmigos`/`PanelGrupos` en `Progreso.tsx` (mismo hueco admin-gated que `Invitaciones`, que se queda intacta) con icono propio `IconoGrupo` (cordada). Verificado con tipos/lint/build/tests; **pendiente de que el usuario pruebe en real** (pegar las reglas nuevas en la consola de Firebase, entrar como propietario, añadirse a sí mismo en «Amigos» y crear un grupo de prueba) antes de seguir con el checkpoint 2.
- **Sistema de grupos de amigos, checkpoint 2 de 2 (el corte)**: `realizados`/`planes`/`comentarios`/`fotos` pasan a llevar `grupoId` y a filtrarse por el grupo activo; se retira por completo el sistema antiguo de `invitados`.
  - **Datos separados por grupo**: `idRealizado` cambia a `${uid}__${grupoId}__${tipo}__${refId}` (el mismo logro cuenta por separado en cada grupo); `escucharRealizados(grupoId, ...)` y `escucharSocial(grupoId, refTipo, refId, ...)` filtran por `grupoId` (tres igualdades en social.ts, sigue sin índice compuesto); `listarPlanes(grupoId)` pierde el `orderBy` del servidor (ordenar `grupoId` + `orderBy` en campos distintos sí lo pediría) y ordena en cliente, como ya hacía `social.ts`. Sin `grupoId`, ninguna de estas funciones se suscribe/consulta (grupo `null` = sin datos, no error).
  - **Grupo activo**: nuevo `src/lib/grupoActivo.ts` (localStorage, patrón de `preferencias.ts`). `MapView.tsx` hidrata `grupoActivoId` una sola vez en cuanto la sesión resuelve (validado contra `sesion.grupos`, cae al primero si el guardado ya no es válido) y lo persiste en cada cambio; selector en la cabecera (icono `IconoGrupo`, desplegable con `IconoDespliegue`) que solo aparece con 2+ grupos — con uno solo no hace falta elegir, y con cero se ve la pastilla «Sin grupo» donde antes decía «Sin invitación». Cambiar de grupo invalida la caché de `planes` para que el planificador vuelva a pedirlos.
  - **Retirada completa de `invitados`**: `src/lib/invitados.ts` y `scripts/invitar.mjs` borrados (seed ahora en `scripts/amigo.mjs`, `npm run amigos:anadir`); `EstadoUsuario` pierde `invitado` y el `admin` antiguo (queda solo `amigo`/`admin`/`propietario`/`grupos`); `Invitaciones` desaparece de `Progreso.tsx` (ya solo quedan `PanelAmigos`/`PanelGrupos`); `firestore.rules` pierde el bloque `invitados` y las funciones `invitado()`/`esAdmin()` de siempre — `esAdminGrupos()` del checkpoint 1 pasa a llamarse simplemente `esAdmin()`. Efecto colateral bueno: `Planificador`/`guardarBorrador` no comprobaban pertenencia al grupo en absoluto antes de este hito (cualquiera con sesión podía guardar un plan); al exigir `grupoId` y que las reglas verifiquen `miembroDeGrupo`, ese hueco se cierra solo (con aviso propio en la UI: «Necesitas pertenecer a un grupo para guardar rutas»).
  - **Sin migración** (decisión ya tomada): los `realizados`/`planes`/`comentarios`/`fotos` de antes de este hito no llevan `grupoId` y quedan huérfanos (no legibles bajo las reglas nuevas); aceptado por el usuario, arranque limpio.
  - Verificado con tipos/lint/build/tests y arrancando `next start` en local. **Pendiente de que el usuario pruebe en real**: pegar las reglas finales en la consola de Firebase, añadirse como amigo y crear al menos un grupo con miembros reales (si no lo hizo ya en el checkpoint 1), y comprobar que marcar/planificar/comentar en dos grupos distintos no se mezcla.
- **Corrección tras probar el sistema de grupos en real**: el usuario detectó que renombrar un grupo no se veía reflejado en las estadísticas, y que no aparecía el selector de grupo activo. Causa raíz: `useUsuario()` (`auth.ts`) resolvía `grupos` una sola vez por inicio de sesión (`listarGruposDe`, `getDocs` puntual) — cualquier cambio posterior desde el panel de administración (renombrar, añadir/quitar miembros) no llegaba a la sesión ya abierta. Arreglado pasando `grupos` (y de paso `amigo`/`admin`) a `onSnapshot` en vivo (`escucharGruposDe` en `grupos.ts`, más un `onSnapshot` sobre el propio doc de `amigos`), con limpieza de las suscripciones en cada cambio de usuario. De paso, mejoras de claridad pedidas en la gestión de grupos: la cabecera muestra siempre el grupo activo (antes solo con 2+ grupos, lo que hacía parecer que la función no existía con 1 o con 0); `PanelGrupos` cambia el "pulsa el nombre para renombrar" (poco descubrible) por un botón «Renombrar» explícito, y añade indicador «tú» + botón «Unirme al grupo»/«Salir del grupo» en cada fila — crear un grupo no te añade a él automáticamente, y sin este atajo no quedaba claro por qué el selector no aparecía tras crear uno.
- **Realizados individuales, además de por grupo**: a petición del usuario, marcar "lo he hecho" ya no exige un grupo activo. `Realizado.grupoId` pasa a `string | null` (`src/lib/realizados.ts`; `idRealizado` incluye `"individual"` en el id cuando no hay grupo) y se añade `escucharRealizadosPropios(uid, ...)` (`where("usuario","==",uid)`, sin filtrar por grupo): es el histórico personal completo (individual + cualquier grupo), independiente del grupo activo.
  - **Formulario**: `MarcarRealizado.tsx` gana una casilla «Marcar como individual» (solo visible si hay grupo activo; sin grupo, se fuerza individual) y un aviso sutil de qué va a pasar («Se compartirá con «Nombre»» / «Se guardará como logro individual, solo tuyo»), tal como pidió el usuario. El registro ya marcado muestra «· individual» si no tiene grupo.
  - **Dos mapas en `MapView.tsx`**: `realizadosGrupo` (del grupo activo, para el ranking/evolución de `Progreso.tsx`) y `realizadosPropios` (histórico personal, para "Lo mío"/picos por altitud/pendientes/por comunidad en `Progreso.tsx`, el estado "ya hecho" de las fichas, `Explorador.tsx` y `planesHechos`). `Progreso.tsx` separa sus cálculos en dos `useMemo` en vez de uno, ya sin necesitar filtrar por `usuario.uid` en el personal (la query ya viene acotada). `puedeMarcar` ya no exige grupo activo, solo sesión + estar en el roster de amigos (ver más abajo).
  - **Reglas de Firestore**: `realizados` distingue lectura/escritura por si `grupoId` es nulo o no (`miembroDeGrupo` para lo compartido; dueño del documento para lo individual). Al revisar el cambio, se detectó que el sign-in de Google no está restringido a nadie en concreto — sin más comprobación, cualquier cuenta de Google (no solo el círculo cerrado) habría podido escribir realizados individuales. Corregido con una función `esAmigo()` nueva (está en el roster `amigos`, o es el propietario), exigida para crear/actualizar un realizado individual; `esAdmin()` pasa a apoyarse en ella. `puedeMarcar` en el cliente también exige `sesion.amigo` para no ofrecer un botón que fallaría en silencio a quien no sea del grupo.
  - Verificado con tipos/lint/build/tests. Pendiente de que el usuario pruebe en real: marcar algo individual y confirmar que no aparece en "El grupo" pero sí en "Lo mío", y pegar las reglas actualizadas en la consola de Firebase.
- **Marcar en grupo con participantes + ventana de actividades del grupo**: al marcar "lo he hecho" para el grupo (no individual), se puede etiquetar a otros miembros como participantes — cada uno se lleva su propio registro real (cuenta para su "Lo mío"; si luego lo desmarca, solo desaparece el suyo, sigue para los demás). El problema de fondo era que `Realizado.usuario` es un uid de Firebase pero los grupos guardan miembros por email: cada amigo autorregistra su `uid`/`nombre` en su propio doc de `amigos` la primera vez que entra tras este cambio (`registrarUid` en `amigos.ts`, llamado solo si cambia, desde el `onSnapshot` que `useUsuario()` ya tenía sobre ese doc); mientras no haya entrado nunca, no es seleccionable ("aún no ha entrado a la app"). `firestore.rules`: lectura de `amigos` abierta a cualquier amigo (antes solo admin o uno mismo, hacía falta para resolver a los compañeros de grupo); autorregistro de `uid`/`nombre` limitado con `diff().affectedKeys().hasOnly([...])` (nunca `admin`); `create` de `realizados` de grupo ya no exige que el `usuario` del documento seas tú, solo que pertenezcas a ese grupo (no se puede verificar en reglas que el uid destinatario sea realmente de alguien del grupo — mismo criterio ya aceptado para `comentarios`/`fotos` a esta escala); `update`/`delete` siguen exigiendo ser el dueño real, así que "desmarcar solo lo mío" sale gratis por los ids deterministas de `idRealizado`. Nuevo `marcarRealizadoGrupo` (`realizados.ts`) hace un alta por destinatario con `Promise.allSettled` (si alguien ya lo tenía marcado, no tumba a los demás). `MapView.tsx` calcula `participantesGrupo` (miembros del grupo activo cruzados con el roster de amigos) y lo pasa a `FichaElemento`/`FichaRuta` → `MarcarRealizado.tsx` (nuevo bloque "También lo hicieron").
  - **Ventana de actividades del grupo**: sustituye al desplegable "El grupo" (que solo mostraba persona+total). Nuevo `ModalActividadesGrupo.tsx`, ventana centrada (mismo patrón que el visor de fotos de `SeccionSocial.tsx`) que agrupa los realizados del grupo por elemento/ruta y muestra, para cada uno, quién participó y cuándo; con una tira de recuento por persona arriba (sustituye de forma ligera al ranking) y un filtro de texto.
  - Verificado con tipos/lint/build/tests. Pendiente de que el usuario pruebe en real: pegar las reglas nuevas, marcar un pico etiquetando a un compañero, comprobar que le aparece en su "Lo mío", y que al desmarcarlo él, sigue apareciendo para los demás en la ventana de actividades.
- **Siguiente**: Hito 22 (estadísticas por comarca) y sitios de interés adicionales, o revisar con el usuario. Selección de área libre para descargar (hoy solo por ruta/plan/actividad) sigue apuntada en `notas-mejoras.md` como mejora futura del Hito 9.

## Comandos

- `npm run dev` — desarrollo (puerto 3000)
- `npm run build` — build de producción (ejecútalo antes de dar por cerrado un cambio)
- `npm test` — batería de pruebas (vitest, sin red); `npm run test:red` — añade integración real contra BRouter/OSRM
- `npm run lint` — linter

## Normas de diseño gráfico (obligatorias)

El acabado visual debe parecer diseñado por una persona, no generado por IA:

1. **PROHIBIDOS los emojis** en cualquier parte de la interfaz, textos de la app, iconografía o placeholders. Sin excepciones.
2. **Iconos propios en SVG**: crea un set de iconos original (pico, ibón, refugio, collado, ruta, etc.) como componentes React con SVG inline, con trazo y estilo coherentes entre sí. No uses librerías de iconos genéricas (Heroicons, Font Awesome, Lucide…) ni emojis como iconos.
3. **Identidad visual original**: paleta propia inspirada en el Pirineo (roca, hielo, pino, ocres) definida como design tokens; nada de gradientes morado-azul genéricos, glassmorphism por defecto, ni composiciones tipo landing de plantilla (hero centrado + tres cards con iconos).
4. **Tipografía deliberada**: elige y justifica las fuentes; no dejar la tipografía por defecto en pantallas acabadas.
5. Ilustraciones, texturas y elementos decorativos: generados a medida en SVG propio, coherentes con el set de iconos.

## Convenciones

- Idioma de la interfaz y del contenido: español.
- Commits con mensajes en español, autoría `multyderpyxd@gmail.com` (ya configurado en el repo).
- Atribución obligatoria y visible en el mapa: «© Instituto Geográfico Nacional» y «© OpenStreetMap contributors».
- Sin dependencias de pago; todo dentro de capas gratuitas (IGN, OSM, Firebase, Vercel).
